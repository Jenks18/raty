import React from 'react';
import { Typography } from '@mui/material';

const PaymentHistory = () => {
  return <Typography variant="h4">PaymentHistory Page</Typography>;
};

export default PaymentHistory;
