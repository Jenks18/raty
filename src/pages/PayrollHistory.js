import React from 'react';
import { Typography } from '@mui/material';

const PayrollHistory = () => {
  return <Typography variant="h4">PayrollHistory Page</Typography>;
};

export default PayrollHistory;
