import React from 'react';
import { Typography } from '@mui/material';

const Transactions = () => {
  return <Typography variant="h4">Transactions Page</Typography>;
};

export default Transactions;
