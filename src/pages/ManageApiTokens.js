import React from 'react';
import { Typography } from '@mui/material';

const ManageApiTokens = () => {
  return <Typography variant="h4">ManageApiTokens Page</Typography>;
};

export default ManageApiTokens;
